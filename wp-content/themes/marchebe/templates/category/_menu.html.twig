{% if children|length > 0 %}
    <nav aria-label="Filtrer les articles par catégorie" class="mx-auto p-8 bg-blue-50">
        <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2">
            {% include '@AcMarche/category/_btn_all.html.twig' with {'item':category, 'name': 'Tous'} %}
            {% for child in children %}
                {% include '@AcMarche/category/_btn_all.html.twig' with {'item':child} %}
            {% endfor %}
        </ul>
    </nav>
{% endif %}