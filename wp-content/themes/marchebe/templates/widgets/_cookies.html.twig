<div x-data="{
    isPopupCookieOpen: false,
    togglePopupCookie() {
        this.isPopupCookieOpen = !this.isPopupCookieOpen;
    },
          }">

    <section
            class="isolate z-30 fixed max-w-md p-5 mx-auto bg-white border border-gray-200 shadow-2xl left-4 md:left-12 bottom-4 md:bottom-16 rounded-2xl">
        <div class="flex items-start gap-3">
            <span class="text-3xl flex-shrink-0">🍪</span>
            <div class="flex-1">
                <h4 class="font-bold text-gray-900 text-lg">Cookie Notice</h4>

                <p class="mt-2 text-gray-700 text-sm leading-relaxed">
                    Ce site utilise des cookies pour vous fournir une expérience optimale.
                    <a href="/administration/rgpd" class="text-citoyen hover:underline font-medium">
                        Mentions légales et RGPD
                    </a>
                </p>

                <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-2 mt-4">
                    <button @click="togglePopupCookie"
                            class="text-sm font-medium text-gray-700 hover:text-gray-900 underline transition-colors focus:outline-none">
                        Personnaliser
                    </button>
                    <button
                            class="text-sm bg-citoyen font-semibold rounded-lg hover:bg-opacity-90 text-white px-5 py-2.5 transition-all focus:outline-none focus:ring-2 focus:ring-citoyen focus:ring-offset-2 shadow-md"
                            @click="togglePopupCookie">
                        Tout accepter
                    </button>
                </div>
            </div>
        </div>
    </section>
    {% include '@AcMarche/widgets/_cookies_popup.html.twig' %}
</div>