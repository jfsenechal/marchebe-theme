<section>
    <section class="bg-slate-50 ">
        {% include '@AcMarche/article/_header.html.twig' %}
        <div class="relative z-20 -mt-16 md:-mt-24">
            <article class="w-full md:max-w-6xl mx-auto bg-white rounded-xl shadow-2xl p-2 sm:p-10 md:p-12">
                {% include '@AcMarche/article/_title.html.twig' %}
                <div class="prose max-w-none prose-lg prose-slate prose-a:text-blue-600 hover:prose-a:text-blue-800">
                    {% if event.description %}
                        <p class="fade-in-up text-xl leading-relaxed text-slate-600">
                            {{ event.description }}
                        </p>
                    {% endif %}

                    <div class="fade-in-up">
                        <p class="text-4xl text-red-500 font-bold text-center">To do details agenda</p>
                        {% include '@AcMarche/agenda/_dates.html.twig' %}
                        <ul class="flex flex-wrap gap-2">
                            {% for tag in event.tagsFormatted %}
                                <li
                                        class="text-sm mx-2 whitespace-nowrap flex items-center border border-caractere text-caractere transition-all duration-500 ease-in-out hover:bg-cta-light hover:text-white">
                                    <a href="#" class="px-2 py-2 ">{{ tag.name }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="fade-in-up mt-8 border-t border-slate-200 pt-8">
                        <p class="text-center text-slate-600">
                            La Ville de Marche-en-Famenne vous remercie de votre compréhension et de votre
                            collaboration.
                        </p>
                    </div>
                </div>
            </article>
        </div>
        {% include '@AcMarche/article/_see_also.html.twig' %}
    </section>
</section>